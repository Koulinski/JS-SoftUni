function lToU(a) {

    let letter = String(a);
    if (letter === letter.toLowerCase()) {
        console.log(`lower-case`);
    } else {
        console.log(`upper-case`);
    }

}
lToU('L')
//lToU('f')