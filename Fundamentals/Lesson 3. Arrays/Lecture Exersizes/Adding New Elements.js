// let arr = [10, 20, 30];
// arr[arr.length] = 40;
// console.log(arr);
// arr.push(50);
// console.log(arr);


// let arr =[10, 20, 30];
// console.log(arr.includes(0));
// console.log(arr.join(':'));

// let words = ['one', 'two'];
// console.log(words.join('-'));


// let capitals = ['Sofia', 'Washington', 'London'];
// for (let i = 0; i < capitals.length; i++) {
// console.log(capitals[i]); }
// console.log(capitals.toString());


/* function printStuff(n, arr) {
    let nums = [];
    for (let i = 0; i < n; i++) {
        nums.push(arr[i]);
        let output = ' ';

        for (let i = nums.length - 1; i >= 0; i--) {
            output += `${nums[i]}`;

        }
        console.log(output);
    }
}

printStuff(3, [10, 20, 30, 40]) */

/* Reverse Array of Strings */

/* function reverse(arr) {
    for (let i = 0; i < arr.length / 2; i++) {
        let oldElement = arr[i];
        let previousIndex = arr.length - 1 - i;
        arr[i] = arr[previousIndex];
        arr[previousIndex] = oldElement;
    }
    console.log(arr.join(''));
}
reverse(['a', 'b', 'c', 'd', 'e']); */

/* Print an Array with For-of */
/* let nums = [1, 2, 3, 4, 5];
let output = '';
for (let num of nums) {
    output += `${num}`;
}
console.log(output); */

